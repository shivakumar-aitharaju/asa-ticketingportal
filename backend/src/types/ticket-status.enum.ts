export enum TicketStatus {
  Open = 'open',
  Assigned = 'assigned',
  InProgress = 'in_progress',
  PendingClient = 'pending_client',
  Resolved = 'resolved',
  Closed = 'closed',
  Escalated = 'escalated',
  Reopened = 'reopened',
}
